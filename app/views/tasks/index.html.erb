<h1>My Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
  <li>
    <%= task.completed ? "☑️" : "◻️" %>
    <%= link_to task.title, task_path(task.id) %>
    <%= link_to "Delete", task_path(task), method: :delete, data: { confirm: "Are you sure you want to delete #{task.title}?" }  %>
  </li>
  <% end %>
</ul>

<div>
  <%= link_to "Add new task", new_task_path %>
</div>
